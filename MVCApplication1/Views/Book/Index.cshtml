@model IEnumerable<Book>

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">
        <div class="col">
            <h2 class="text-primary">Book List</h2>
        </div>
        <div class="col text-end">
            <a asp-controller="Book" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Book
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped" style="width: 100%">
        <thead>
            <tr>
                <th>
                    Book Name
                </th>
                <th>
                    Author Name
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
           {
               <tr>
                   <td width= 50%>
                       @item.Title
                   </td>
                   <td width= 30%>
                       @item.Author
                   </td>
                   <td>
                       <div class="btn-group">
                           <a asp-controller="Book" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-secondary">
                               <i class="bi bi-pencil-square"></i>
                           </a>
                           <a asp-controller="Book" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">
                               <i class="bi bi-trash-fill"></i>
                           </a>
                       </div>
                   </td>
               </tr>
           }
        </tbody>
    </table>
</div>



